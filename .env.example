# LLM provider: claude-cli (default) | claude | openai
# claude-cli: Uses your Claude Code subscription via CLI (no API key needed)
# claude: Uses Anthropic API directly (requires ANTHROPIC_API_KEY)
LLM_PROVIDER=claude-cli

# Required only for LLM_PROVIDER=claude
# ANTHROPIC_API_KEY=sk-ant-...

# Required for LLM_PROVIDER=claude-cli inside Docker
# Get token via: claude setup-token
# Or extract from Keychain: security find-generic-password -s "Claude Code-credentials" -a "$(whoami)" -w | python3 -c "import sys,json; print(json.load(sys.stdin)['claudeAiOauth']['accessToken'])"
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-...

# REST API server settings (used by npm run dev:server / Docker)
# API_KEY=my-secret-api-key    # Required for authenticated API access
# PORT=3000                     # Server port (default: 3000)
